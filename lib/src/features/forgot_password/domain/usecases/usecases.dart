import '../repositories/repositories.dart';

class GetForgot_passwordUseCase {
  final Forgot_passwordRepository repository;

  GetForgot_passwordUseCase({required this.repository});

  // Future<User> execute(String userId) async {
  //   return userRepository.getUser(userId);
  // }
}
