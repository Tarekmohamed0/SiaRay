import '../repositories/repositories.dart';

class GetStatic_screenUseCase {
  final Static_screenRepository repository;

  GetStatic_screenUseCase({required this.repository});

  // Future<User> execute(String userId) async {
  //   return userRepository.getUser(userId);
  // }
}
